[
  {
    "file": "packages/verify/src/grade/grade.ts",
    "find": "    truth.notDerived.filter((n) => n.metric === undefined).map((n) => [n.category, n.reason]),",
    "replace": "    truth.notDerived.map((n) => [n.category, n.reason]),"
  }
]
