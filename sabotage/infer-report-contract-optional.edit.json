[
  {
    "file": "packages/schema/src/stage-report.ts",
    "find": "    if (report.stage === 'infer' && report.detail === undefined) {\n      ctx.addIssue({\n        code: 'custom',\n        path: ['detail'],\n        message:\n          'an infer stage report carries the scored-field contract (0015 §7.1) — what is measured is reported before how it did',\n      });\n    }",
    "replace": ""
  }
]
