[
  {
    "file": "packages/schema/src/seed-state.ts",
    "find": "  return a.source === 'fixture-seed' && b.source === 'fixture-seed' && a.id === b.id;",
    "replace": "  return a.source === 'fixture-seed' && b.source === 'fixture-seed';"
  }
]
