[
  {
    "file": "packages/verify/src/grade/compare.ts",
    "find": "  } else if (!sameSeedState(before.seedState, after.seedState)) {",
    "replace": "  } else if (sameSeedState(before.seedState, after.seedState) === false) {"
  }
]
