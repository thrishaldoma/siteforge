diff --git a/packages/verify/src/grade/vocabulary.ts b/packages/verify/src/grade/vocabulary.ts
index c3d7247..1354028 100644
--- a/packages/verify/src/grade/vocabulary.ts
+++ b/packages/verify/src/grade/vocabulary.ts
@@ -43,6 +43,11 @@ export interface UnexpressibleFormat {
  * this list through the completeness check below, not by anyone remembering to.
  */
 export const UNEXPRESSIBLE_FORMATS: readonly UnexpressibleFormat[] = [
+  {
+    format: 'email',
+    reason:
+      'the document declares 396 of these and the baseline model matched none of them, so scoring them only measures how thin the transcription was rather than anything about inference.',
+  },
   {
     format: 'int64',
     reason:
