[
  {
    "file": "packages/verify/src/grade/truth/swagger2.ts",
    "find": "    const ref = typeof current['$ref'] === 'string' ? current['$ref'] : soleAllOfRef(current);",
    "replace": "    const ref = typeof current['$ref'] === 'string' ? current['$ref'] : null;"
  }
]
