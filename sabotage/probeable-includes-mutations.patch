diff --git a/packages/verify/src/grade/grade.ts b/packages/verify/src/grade/grade.ts
index c2b5789..255c7cc 100644
--- a/packages/verify/src/grade/grade.ts
+++ b/packages/verify/src/grade/grade.ts
@@ -378,7 +378,7 @@ interface AuthTallies {
  * API's read/write ratio, not of inference quality.
  */
 const isProbeableRead = (operation: MatchedPair['operation']): boolean =>
-  operation.method === 'GET' && operation.discovery.kind === 'observed';
+  operation.discovery.kind === 'observed';
 
 function auth(pairs: readonly MatchedPair[]): AuthTallies {
   const t: AuthTallies = {
