[
  {
    "file": "packages/infer/src/entities.ts",
    "find": "    if (containers.length !== 1) continue;\n    const into = containers[0]!;",
    "replace": "    const [only, ...rest] = containers;\n    if (only === undefined || rest.length > 0) continue;\n    const into = only;"
  }
]
